<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Cart</title>
</head>
<body>
  <h1>Cart Details</h1>
  <p>Cart ID: {{ cart._id }}</p>
  
  <h2>Products in Cart</h2>
  <ul>
    {{#each cart.products}}
      <li>{{ this.name }} - Price: {{ this.price }}</li>
    {{/each}}
  </ul>
</body>
</html>
